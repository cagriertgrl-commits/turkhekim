export const metadata = {
  title: "Doktor Paneli",
  description: "DoktorPusula doktor yonetim paneli. Randevular, yorumlar, profil ve istatistikler.",
};

export default function Layout({ children }) {
  return children;
}
